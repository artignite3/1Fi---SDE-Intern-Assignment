# Categories API Endpoint (src/app/api/categories/)

Provides a REST API endpoint to retrieve all product categories available within the 1Fi Marketplace.

---

## Endpoint Details

- **Route**: GET /api/categories
- **Response Format**: JSON array of Category objects including category ID, display name, product count, and vector icon identifier.
