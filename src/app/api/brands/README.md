# Brands API Endpoint (src/app/api/brands/)

Provides a REST API endpoint to retrieve partner merchant brands for the Top Brands directory and search functionality.

---

## Endpoint Details

- **Route**: GET /api/brands
- **Query Parameters**:
  - query *(optional, string)*: Filter brands by name or category keyword.
- **Response Format**: JSON array of Brand objects.
